{
  "name": "chrome-ext-search",
  "version": "1.0.0",
  "description": "Chrome extension for enhanced page search",
  "main": "background.js",
  "scripts": {
    "build": "node build.js",
    "watch": "nodemon --watch src --watch manifest.json --watch popup.html --ext js,css,html,json --exec \"npm run build\""
  },
  "keywords": [
    "chrome",
    "extension",
    "search"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
} 